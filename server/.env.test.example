PORT=4000

# Use a separate database from dev so running tests never touches real data.
PGHOST=localhost
PGPORT=5432
PGUSER=expenseflow
PGPASSWORD=expenseflow
PGDATABASE=expenseflow_test

JWT_SECRET=test-secret
JWT_EXPIRES_IN=7d
CLIENT_ORIGIN=http://localhost:5173
