VITE_API_URL=http://localhost:4000/api
# On Vercel, set VITE_API_URL=/api instead (client and server share one domain
# there, via vercel.json's routing, so a relative path is correct — no CORS
# needed in production either).
